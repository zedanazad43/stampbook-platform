{
  "name": "stampcoin-platform",
  "version": "2.0.0",
  "description": "Stampcoin Digital Platform - Blockchain-based digital stamps platform with wallet and marketplace",
  "main": "server.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "mkdir -p public && echo 'Static site ready for deployment'",
    "test": "jest tests/*.test.js",
    "lint": "echo 'Linting can be configured'",
    "docker:build": "docker build -t stampcoin-platform .",
    "docker:run": "docker run -p 8080:8080 stampcoin-platform"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17",
    "jest": "^30.2.0",
    "nodemon": "^3.1.14"
  }
}

