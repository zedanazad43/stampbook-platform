name: "Stampcoin CodeQL Config"

# Only scan JavaScript/TypeScript — the only languages in this repository.
# This prevents the default CodeQL setup from attempting to analyze
# languages (Go, Ruby, Python) for which there are no source files.
disable-default-queries: false

queries:
  - uses: security-and-quality

paths-ignore:
  - node_modules
